apiVersion: v1
kind: Service
metadata:
  name: {{PROJECT_NAME}}
  namespace: {{NAMESPACE}}
  labels:
    app: {{PROJECT_NAME}}
    environment: {{STAGE}}
spec:
  selector:
    app: {{PROJECT_NAME}}
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP