apiVersion: v1
kind: Secret
metadata:
  name: {{PROJECT_NAME}}-secret
  namespace: {{NAMESPACE}}
type: Opaque
data:
  # Adicione os valores sensíveis em base64 abaixo
  # Exemplo: echo -n "minha-senha" | base64
  # DB_PASSWORD: <base64-encoded-value>
  # API_KEY: <base64-encoded-value>